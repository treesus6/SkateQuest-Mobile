import React from 'react';
import { View } from 'react-native';

interface Props { skateparkName?: string; }

// Portal Dimension logo placeholder — swap out for real asset when ready
export default function PortalDimensionLogo(_props: Props) {
  return <View />;
}
