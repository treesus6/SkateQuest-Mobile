name: Build SkateQuest APK
# Disabled — builds are handled via EAS directly
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use EAS to build. Run eas build --platform android --profile production"
